


<!DOCTYPE html>
<html>
    <head>
        <title>YelpCamp</title>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css">
        <link rel="stylesheet" href="/stylesheets/main.css">
    </head>
    <body>
    <nav class="navbar navbar-default">
        <div class="container-fluid">
            <div class="navbar-header">
                <a class="navbar-brand" href="/">YelpCamp</a>
            </div>
            <div class="collapse navbar-collapse">
                <ul class="nav navbar-nav navbar-right">
                    
                  <%  if(!currentUser){ %> <!--currentUser omes from the index section of app.js and this if statement chacks if somebody is logged in-->
                    <li><a href="/login">Login</a></li>
                    <li><a href="/register">Sign Up</a></li>
                  <%  } else { %>
                   <li><a href="#" >Signed In as <%=currentUser.username %> </a></li><!--the a tag for was styling purposes and pound sign means it wont take u anywhere-->
                    <li><a href="/logout">Logout</a></li>
                  <%  } %>
                    
                </ul>
            </div>
        </div>
    </nav>
    
    <div class="container">
        <% if (error&& error.length>0){%>  <!--will hide or show flash message, the .length is to make sure it's not an empty array-->
          <div class="alert alert-danger" role="alert"><%=error%></div> <!--this comes from the index route -->
        <% }%>
          
          <% if (success&& success.length>0){%>  <!--will hide or show flash message, the .length is to make sure it's not an empty array-->
          <div class="alert alert-success" role="alert"><%=success%></div> <!--this comes from the index route -->
          <% }%>
    </div>
 
    
    